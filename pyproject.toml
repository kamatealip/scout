[project]
name = "scout"
version = "0.1.0"
description = "A local documentation search application built with Flask for searching HTML documentation files."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
]
